---
# deploy-windows.yml — Filebeat sur Windows (collection ansible.windows).
- name: Filebeat — Windows
  hosts: windows
  gather_facts: true
  become: false              # Windows : pas de sudo (session SSH admin déjà élevée)
  roles:
    - role: filebeat_windows
